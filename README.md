## Web Base

### Features:
- *Browserify* allow us to use npm packages and bundle the js in one file.
- *ESLint* allow us get errors when programming (check Standard).
- *Bootstrap* makes the styles easy.

### Run command:
```
gulp
```